decoration {
    rounding       = 12
    rounding_power = 4

    active_opacity   = 0.95
    inactive_opacity = 0.9

    shadow {
        range        = 42
        render_power = 2
        color        = 0x551a1a1a
    }

    blur {
        size   = 10
        passes = 2
        xray   = true
        popups = true
    }
}
